#define CODE_FOR_HEAP
#include "heap.h"
#include "utilities/stdlib/stdlib.h"


int heap_create(struct heap* heap, void *ptr, void *end, struct heap_table* table)
{
    // restituisce 0 se e' tutto ok, < di 0 se qualcosa e' andato storto

    return 0;
}